FEEDSTRINGS;
SKIPCHECK;
#Region : Rule Header
#############################
##~~ Start Rule File ~~##
#############################
# DEVELOPER : Steve Waterbury
# CREATED ON : 2026-04-23
# LAST UPDATE: 2026-04-30
# CUBE       : Merch Version SKU Attributes
#
#### CHANGE HISTORY:
### 2026-04-23  Steve Waterbury  Initial rule file — Target_WOS_Applied cascade (own SKU Override > parent Override > 0)
### 2026-04-30  Steve Waterbury  Use_Budget_After_Horizon Applied cascade (mirrors Target_WOS pattern; String S: rule, no feeder needed under FEEDSTRINGS+SKIPCHECK)
#
#EndRegion

#Region : Target_WOS Cascade
['Target_WOS_Applied'] = N:
  IF(DB('Merch Version SKU Attributes', !Merch Version, !Merch SKU, 'Target_WOS_Override') @<> '',
     NUMBR(DB('Merch Version SKU Attributes', !Merch Version, !Merch SKU, 'Target_WOS_Override')),
  IF(ELPAR('Merch SKU', !Merch SKU, 1) @= '', 0,
  IF(DB('Merch Version SKU Attributes', !Merch Version, ELPAR('Merch SKU', !Merch SKU, 1), 'Target_WOS_Override') @<> '',
     NUMBR(DB('Merch Version SKU Attributes', !Merch Version, ELPAR('Merch SKU', !Merch SKU, 1), 'Target_WOS_Override')),
     0)));
#EndRegion

#Region : Use_Budget_After_Horizon Cascade
['Use_Budget_After_Horizon Applied'] = S:
  IF(DB('Merch Version SKU Attributes', !Merch Version, !Merch SKU, 'Use_Budget_After_Horizon Override') @<> '',
     DB('Merch Version SKU Attributes', !Merch Version, !Merch SKU, 'Use_Budget_After_Horizon Override'),
  IF(ELPAR('Merch SKU', !Merch SKU, 1) @= '', '',
     DB('Merch Version SKU Attributes', !Merch Version, ELPAR('Merch SKU', !Merch SKU, 1), 'Use_Budget_After_Horizon Override')));
#EndRegion

FEEDERS;
['Target_WOS_Override'] => ['Target_WOS_Applied'];

#Region : Rule Footer
#############################
##~~ End Rule File ~~##
#############################
#EndRegion